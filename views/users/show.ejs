<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= 'Fazbook - ' + user.firstName "'s Profile" %></title>
</head>
<body>
  <h1><% user.firstName + ' ' + user.lastName %></h1>
  <h3>Email: <%= user.email %></h3>
  <h3>Date of birth: <%= moment(user.dob).format('LL')%></h3>
  <a href="<%= '/users/' + user.id + '/edit' %>">Edit User info</a>
  <a href="/users">Back to users</a>

</body>
</html>
